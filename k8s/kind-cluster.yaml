# kind cluster config for local learning/ArgoCD experiments.
# Single control-plane node is plenty for this project.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: mini-settlement
nodes:
  - role: control-plane
